/**
 * The heading levels Markdown names. Both directions of this package need them — the rules
 * one per level, the parser to clamp a depth a tree should not produce — so the set lives
 * in one place rather than in two.
 */
export const HEADING_LEVELS = [1, 2, 3, 4, 5, 6] as const
