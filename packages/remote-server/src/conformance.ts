/**
 * The conformance suite moved to `foldkit-entity/conformance`, beside the
 * reference interpreter whose agreement it checks.
 *
 * Re-exported here because this is where interpreters found it.
 */
export {
  Subject,
  cases,
  rows,
  type ConformanceCase,
  type ConformanceRow,
} from 'foldkit-entity/conformance'
